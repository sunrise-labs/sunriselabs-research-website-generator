<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <meta name="theme-color" content="#0a0a0a" media="(prefers-color-scheme: dark)">
    <meta name="theme-color" content="#fafafa" media="(prefers-color-scheme: light)">
    <title>{{title}} - Sunrise Labs</title>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;500;600&family=IBM+Plex+Mono:wght@300;400;500;600&family=Spectral:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/main.css">
</head>
<body>
    <div class="dot-grid"></div>

    {{> nav}}

    <div class="page-container">
        {{#if breadcrumbs}}
        <div class="breadcrumb">
            {{#each breadcrumbs}}
                {{#if current}}
                    <span class="breadcrumb-current">{{title}}</span>
                {{else}}
                    <a href="{{url}}">{{title}}</a>
                    <span class="breadcrumb-separator">/</span>
                {{/if}}
            {{/each}}
        </div>
        {{/if}}

        <div class="page-grid">
            {{> sidebar}}

            <main class="content">
                <div class="doc-header">
                    <div class="doc-type">{{capitalize type}}</div>
                    <h1>{{title}}</h1>
                    <div class="doc-meta-line">
                        <span>Updated: {{date}}</span>
                    </div>
                </div>

                {{{content}}}
            </main>
        </div>
    </div>

    <script src="/assets/js/theme-toggle.js"></script>
</body>
</html>
